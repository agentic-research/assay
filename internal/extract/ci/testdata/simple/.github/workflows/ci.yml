name: CI

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Run tests
        run: go test ./...
      - name: Pull base image
        run: docker pull gcr.io/distroless/static-debian12
